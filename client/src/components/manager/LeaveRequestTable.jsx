const formatDate = (date) => new Intl.DateTimeFormat("en", { month: "short", day: "numeric", year: "numeric" }).format(new Date(date));

const LeaveRequestTable = ({ requests, onDecide, showAppliedDate = false }) => <div className="table-wrap"><table><thead><tr><th>Employee</th><th>Leave type</th><th>Start date</th><th>End date</th><th>Duration</th><th>Reason</th>{showAppliedDate && <th>Applied date</th>}<th>Status</th>{onDecide && <th>Actions</th>}</tr></thead><tbody>{requests.map((request) => <tr key={request._id}><td><strong>{request.employee?.name || "Former employee"}</strong><small>{request.employee?.email}</small></td><td><span className="leave-type">{request.leaveType}</span></td><td>{formatDate(request.startDate)}</td><td>{formatDate(request.endDate)}</td><td>{request.totalDays} day{request.totalDays === 1 ? "" : "s"}</td><td className="table-cell--reason">{request.reason}</td>{showAppliedDate && <td>{formatDate(request.createdAt)}</td>}<td><span className={`status status--${request.status.toLowerCase()}`}>{request.status}</span></td>{onDecide && <td><div className="table-actions"><button className="text-button" type="button" onClick={() => onDecide(request, "approve")}>Permit</button><button className="text-button text-button--gold" type="button" onClick={() => onDecide(request, "reject")}>Reject</button></div></td>}</tr>)}</tbody></table></div>;

export default LeaveRequestTable;
